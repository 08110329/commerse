"use client";

import { PropsWithChildren } from "react";

interface Products {
  title: string;
  price: string;
  image: string;
  description: string;
  size: string;
  color: string;
  tshirheg: string;
  productCode: string;
  torolId: string;
}

export const ProductProvider = ({ children }: PropsWithChildren) => {};
